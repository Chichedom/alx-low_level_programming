#include "main.h"
#include <stdio.h>

/**
 * _abs - function computes the absolute value of an integer.
 * @n: The integer input to be computed.
 * Return: The computed absolute value of the integer.
 */
int _abs(int n)
{
if (n >= 0)
return (n);
else
return (-n);
}
